<Context>
        <Resource
                name="jdbc/mysql"
                auth="Container"
                type="javax.sql.DataSource"
                maxActive="100"
                maxIdle="30"
                maxWait="10000"
                username="root"
                password="alaska"
                driverClassName="com.mysql.jdbc.Driver"
                url="jdbc:mysql://localhost:3306/alaska"/>
        <!-- When deploying to the server make sure that the url is jdbc:mysql://localhost:3306/alaska -->
        <!-- Todo: Set up mysql server with remote access to enable local development -->
</Context>